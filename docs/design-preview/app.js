const $=s=>document.querySelector(s);
const icons={grid:'<rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/>',book:'<path d="M4 4h6a3 3 0 0 1 3 3v14a4 4 0 0 0-4-2H4V4Zm16 0h-4a3 3 0 0 0-3 3v14a4 4 0 0 1 4-2h3V4Z"/>',inbox:'<path d="m3 14 4-10h10l4 10v6H3v-6Z"/><path d="M3 14h5l2 3h4l2-3h5"/>',mail:'<rect x="3" y="5" width="18" height="14" rx="2"/><path d="m3 6 9 7 9-7"/>',users:'<circle cx="9" cy="8" r="3"/><path d="M3 20v-2a6 6 0 0 1 12 0v2M16 5a3 3 0 0 1 0 6m2 3a5 5 0 0 1 3 5"/>',chart:'<path d="M4 3v17h17M8 16v-5m5 5V7m5 9V4"/>',list:'<rect x="5" y="3" width="14" height="18" rx="2"/><path d="M9 8h6m-6 4h6m-6 4h4"/>',settings:'<path d="m9 3-1 3-3 1-2 4 2 3v4l4 3 3-2 3 2 4-3v-4l2-3-2-4-3-1-1-3H9Z"/><circle cx="12" cy="12" r="3"/>',shield:'<path d="m12 3 8 3v6c0 5-8 9-8 9s-8-4-8-9V6l8-3Z"/><path d="m8 12 3 3 5-6"/>',info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v6m0-10v.2"/>',plus:'<path d="M12 5v14M5 12h14"/>',arrow:'<path d="M4 12h15m-6-6 6 6-6 6"/>',chevron:'<path d="m9 5 7 7-7 7"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',pause:'<path d="M8 5v14M16 5v14"/>',play:'<path d="m7 4 13 8-13 8V4Z"/>',check:'<path d="m5 12 4 4L19 6"/>',refresh:'<path d="M20 7a9 9 0 1 0 1 9M20 3v5h-5"/>',search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 6 6"/>',alert:'<path d="m12 3 10 18H2L12 3Z"/><path d="M12 9v5m0 3v.1"/>',download:'<path d="M12 3v12m-5-5 5 5 5-5M4 17v4h16v-4"/>',star:'<path d="m12 3 3 6 7 1-5 5 1 7-6-3-6 3 1-7-5-5 7-1 3-6Z"/>',close:'<path d="m6 6 12 12M18 6 6 18"/>',file:'<path d="M14 3H5v18h14V8l-5-5Zm0 0v5h5M9 13h6m-6 4h6"/>',send:'<path d="m21 3-7 18-4-8-8-4 19-6ZM10 13 21 3"/>',calendar:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M7 3v4m10-4v4M3 11h18"/>',heart:'<path d="M20 5a5 5 0 0 0-8 1 5 5 0 0 0-8-1c-6 6 8 15 8 15s14-9 8-15Z"/>'};
const icon=(name)=>`<svg class="icon" viewBox="0 0 24 24" aria-hidden="true">${icons[name]||icons.file}</svg>`;
function hydrate(){document.querySelectorAll('[data-icon]').forEach(el=>el.innerHTML=icon(el.dataset.icon));}
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const names={dashboard:'工作台',plans:'投稿计划',replies:'编辑回复',logs:'发送记录',stats:'投稿统计',editors:'编辑库',groups:'编辑组',accounts:'发件邮箱',settings:'设置',about:'关于熊猫投稿'};
let page='dashboard',planFilter='全部计划',replyFilter='人工回复',selectedReply=0,paused=false,editorFilter='全部类型',settingsTab='发送设置';
let plans=[{title:'山雨退婚',short:'山雨',genre:'古言 · 情感',words:'12,459',state:'发送中',sent:24,total:36,color:'',time:'今天 14:20',mode:'立即发送'},{title:'长街有风',short:'长街',genre:'现言 · 治愈',words:'18,260',state:'待发送',sent:0,total:24,color:'sand',time:'明天 09:00',mode:'定时发送'},{title:'故人不渡',short:'故人',genre:'古言 · 重生',words:'10,860',state:'草稿',sent:0,total:18,color:'blue',time:'昨天 20:36',mode:'尚未发送'},{title:'春山来信',short:'春山',genre:'现言 · 情感',words:'15,200',state:'已完成',sent:28,total:28,color:'sand',time:'09/21 16:45',mode:'立即发送'}];
const replies=[{name:'青禾',platform:'知微故事',initial:'青',title:'Re: 投稿《山雨退婚》',snippet:'故事收到了，人物关系很有张力，想和你聊聊…',time:'14:32',work:'山雨退婚',tag:'人工回复',body:'你好，林间：<br><br>故事已经读完了，女主的人物转变很有张力，结尾也很有余味。<br><br>我们对这篇稿子很感兴趣，想和你聊一聊前半段的节奏调整。方便的话，请补充一下你的联系方式，我们进一步沟通。<br><br>期待你的回复。<br>青禾',email:'qinghe@example.com'}, {name:'南枝',platform:'拾光阅读',initial:'南',title:'Re:《春山来信》审稿反馈',snippet:'你好，这篇作品已通过初审，请确认授权情况。',time:'11:08',work:'春山来信',tag:'已标记过稿',body:'你好，林间：<br><br>《春山来信》已通过初审，感谢你的来稿。<br><br>请确认作品的授权情况，并提供联系方式，后续我们会与你沟通修改与签约细节。<br><br>南枝',email:'nanzhi@example.com'}, {name:'木棉',platform:'望山文学',initial:'木',title:'Re: 投稿《山雨退婚》',snippet:'感谢来稿，这篇暂时不太符合我们近期的收稿方向。',time:'昨天',work:'山雨退婚',tag:'人工回复',body:'你好：<br><br>感谢来稿。这篇暂时不太符合我们近期的收稿方向，期待你的其他作品。<br><br>祝创作顺利。<br>木棉',email:'mumian@example.com'}, {name:'知微收稿',platform:'知微故事',initial:'知',title:'自动回复：来稿已收到',snippet:'来稿已收到，我们将在 7 个工作日内审阅。',time:'昨天',work:'山雨退婚',tag:'自动回复',body:'你的来稿已收到，我们将在 7 个工作日内审阅。<br><br>这是一封自动回复。',email:'submit@example.com'}, {name:'邮件服务',platform:'投递通知',initial:'邮',title:'投递失败通知',snippet:'收件地址不存在，请检查编辑邮箱。',time:'昨天',work:'山雨退婚',tag:'退信',body:'邮件未能投递，原因：收件地址不存在。<br><br>请核对编辑邮箱后再重新发送。',email:'mailer-daemon@example.com'}];
const editors=[['青禾','知微故事','古言','情感','qinghe@example.com','偏好强情节，7 个工作日内回复'],['南枝','拾光阅读','现言','治愈','nanzhi@example.com','8,000–20,000 字，完稿优先'],['木棉','望山文学','古言','重生','mumian@example.com','收短篇，人物动机要清晰'],['白露','云间故事','悬疑','现言','bailu@example.com','悬念开篇，节奏紧凑'],['初夏','晴川阅读','情感','治愈','chuxia@example.com','温暖、日常题材'],['时雨','听雨书房','古言','情感','shiyu@example.com','不收同人，接受新人投稿']];
const button=(label,action,ico='',cls='')=>`<button class="button ${cls}" data-action="${action}">${ico?icon(ico):''}${label}</button>`;
const badge=(s)=>`<span class="badge ${/待发送|过稿/.test(s)?'gold':/草稿|已完成|自动/.test(s)?'gray':s==='退信'?'red':''}">${s==='发送中'?'<i class="dot"></i>':''}${s}</span>`;
const book=(p)=>`<span class="book-cover ${p.color}">${p.short}</span>`;
const search=(id,placeholder)=>`<label class="search">${icon('search')}<input id="${id}" placeholder="${placeholder}" aria-label="${placeholder}"></label>`;
function heading(title,sub,actions=''){return `<div class="page-heading"><div><h1>${title}</h1><p>${sub}</p></div><div class="heading-actions">${actions}</div></div>`;}
function metrics(items){return `<section class="metrics">${items.map(([label,value,note,ico,action])=>`<button class="metric" data-action="${action}"><span class="metric-label">${label}${icon(ico)}</span><div class="metric-number">${value}<em>封</em></div><div class="metric-note">${note}</div></button>`).join('')}</section>`;}
function replyItem(r,i,inbox=false){return `<button class="reply-item ${inbox&&selectedReply===i?'selected':''}" data-action="reply-${i}"><span class="avatar">${r.initial}</span><span class="reply-main"><span class="reply-top"><b>${r.name}<span class="muted"> · ${r.platform}</span></b><time>${r.time}</time></span><p>${r.snippet}</p><span class="reply-bottom"><small>《${r.work}》</small>${badge(r.tag)}</span></span></button>`;}
function activePlan(){const p=plans[0];return `<div class="panel active-plan"><div class="plan-title-row">${book(p)}<div><div class="plan-title">${p.title}</div><p class="plan-meta">${p.genre} <span> · </span> ${p.words} 字</p></div>${badge(paused?'已暂停':'发送中')}</div><div class="progress-line"><span>投递进度</span><b>24 <span>/ 36 位编辑</span></b></div><div class="progress segmented"><i style="width:66.66%"></i></div><div class="run-hint"><span>${paused?'计划已暂停，可随时继续':'下一封将在 02:18 后发出'}</span><span>每封间隔 100–240 秒</span></div><div class="plan-footer"><span class="senders">${icon('mail')}2 个邮箱轮流发送<span>·</span>已发送 24 封</span><div class="actions">${button(paused?'继续':'暂停','pause',paused?'play':'pause','small ghost')}${button('查看详情','detail-0','arrow','small')}</div></div></div>`;}
function planRows(items,compact=false){return items.map(p=>{const i=plans.indexOf(p);return `<tr><td><div class="table-title">${book(p)}<div><b>${esc(p.title)}</b><small>${p.genre} · ${p.words} 字</small></div></div></td><td>${badge(i===0&&paused?'已暂停':p.state)}</td><td><div class="table-progress"><span class="mono">${p.sent} / ${p.total}</span><div class="progress"><i style="width:${p.sent/p.total*100}%"></i></div></div></td>${!compact?`<td><span style="font-size:11px">${p.mode}</span><small class="muted" style="display:block;font-size:10px">${p.time}</small></td>`:''}<td><button class="text-button" data-action="${p.state==='草稿'?'edit-'+i:'detail-'+i}">${p.state==='草稿'?'继续编辑':'查看详情'}${icon('chevron')}</button></td></tr>`;}).join('')||`<tr><td colspan="5" class="empty">没有匹配的投稿计划</td></tr>`;}
function dashboard(){return heading('工作台','把好故事，送到合适的人手中。',`<span class="date">${icon('calendar')}9 月 23 日 · 星期三</span>${button('新建投稿','new','plus','primary')}`)+metrics([['今日已发送','24','<strong>1 个计划</strong> 正在有序投递','send','go-logs'],['人工回复','3','优先查看编辑的真实反馈','inbox','go-replies'],['过稿回复','1','已手动标记过稿的来信','check','accepted'],['今日发送失败','0','今日投递正常','shield','go-logs']])+`<div class="dashboard-grid"><section><div class="section-head"><h2>正在投递 <span>1 个计划</span></h2><button class="text-button" data-action="go-plans">全部计划${icon('arrow')}</button></div>${activePlan()}<div class="panel queued-plan">${book(plans[1])}<div><h3>长街有风</h3><p>明天 09:00 开始 · 24 位编辑</p></div>${badge('待发送')}<button class="icon-button" aria-label="查看长街有风" data-action="detail-1">${icon('chevron')}</button></div></section><section><div class="section-head"><h2>编辑的来信 <span>人工回复优先</span></h2><button class="text-button" data-action="go-replies">查看全部${icon('arrow')}</button></div><div class="panel reply-panel">${replies.slice(0,3).map((r,i)=>replyItem(r,i)).join('')}</div></section></div><section class="lower-section"><div class="section-head"><h2>最近的投稿计划</h2><button class="text-button" data-action="go-plans">管理计划${icon('arrow')}</button></div><div class="panel table-wrap"><table><thead><tr><th>作品 / 类型</th><th>状态</th><th>投递进度</th><th>发送安排</th><th>操作</th></tr></thead><tbody>${planRows(plans.slice(1))}</tbody></table></div></section>`;}
function plansPage(){return heading('投稿计划','从一篇新稿，到每一次投递。',button('新建投稿计划','new','plus','primary'))+`<div class="plans-summary"><span>共 <strong>${plans.length}</strong> 个计划</span><span><i class="dot"></i> 1 个正在发送</span><span>1 个等待开始</span><span>${plans.filter(p=>p.state==='草稿').length} 个草稿</span></div><div class="filters"><div class="tabs">${['全部计划','发送中','待发送','草稿','已完成'].map(x=>`<button class="${x===planFilter?'on':''}" data-action="plan-filter-${x}">${x}</button>`).join('')}</div>${search('plan-search','搜索作品名称')}</div><div class="panel table-wrap"><table><thead><tr><th>作品 / 类型</th><th>当前状态</th><th>投递进度</th><th>发送安排</th><th>操作</th></tr></thead><tbody id="plan-rows">${planRows(plans.filter(p=>planFilter==='全部计划'||p.state===planFilter))}</tbody></table><div class="list-footer"><span>点击作品详情，查看每一封邮件的发送结果</span><span>共 ${plans.length} 个计划</span></div></div><div class="wizard-note">${icon('info')} 邮件按设定间隔依次发送。你可以随时暂停，也可以关闭窗口，让投稿在托盘继续进行。</div>`;}
function repliesPage(){let filtered=replies.map((r,i)=>({r,i})).filter(({r})=>replyFilter==='全部回复'||(replyFilter==='人工回复'?['人工回复','已标记过稿'].includes(r.tag):replyFilter==='过稿回复'?r.tag==='已标记过稿':r.tag===replyFilter));if(!filtered.some(x=>x.i===selectedReply))selectedReply=filtered[0]?.i||0;let r=replies[selectedReply];return heading('编辑回复','让值得关注的来信，不被自动回复淹没。',button('检查收件箱','scan','refresh','primary'))+`<div class="filters"><div class="tabs">${['人工回复','过稿回复','自动回复','退信','全部回复'].map(x=>`<button class="${replyFilter===x?'on':''}" data-action="reply-filter-${x}">${x}</button>`).join('')}</div><span class="muted" style="font-size:10px">上次检查：今天 14:35</span></div><div class="panel inbox-layout"><div class="inbox-list">${filtered.map(({r,i})=>replyItem(r,i,true)).join('')}</div><article class="inbox-reader"><div class="reader-heading"><h2>${r.title}</h2>${badge(r.tag)}</div><div class="reader-meta"><span class="avatar">${r.initial}</span><div>${r.name} · ${r.platform}<small>${r.email} → linjian@example.com</small></div><small style="margin-left:auto;color:#8d9b90">${r.time}</small></div><div class="mail-body">${r.body}</div><div class="related-work">${icon('book')}关联投稿：《${r.work}》<button class="text-button" data-action="detail-${r.work==='春山来信'?3:0}">查看计划${icon('arrow')}</button></div><div class="reader-actions">${button(r.tag==='已标记过稿'?'取消过稿标记':'标记为过稿','mark-accepted','check',r.tag==='已标记过稿'?'':'primary')}${button('收藏这位编辑','favorite-reply','star')}</div></article></div>`;}
function editorsPage(){return heading('编辑资料库','按作品类型找编辑，把常投的人收进同一组。',button('导入 / 导出','data-editors','download')+button('添加编辑','add-editor','plus','primary'))+`<div class="filters"><div class="tabs"><button class="on" data-action="editors-all">全部编辑 <span>6</span></button><button data-action="editors-groups">编辑组 <span>2</span></button><button data-action="editors-favorites">我的收藏</button></div>${search('editor-search','搜索编辑、平台或邮箱')}</div><div class="editor-chips">${['全部类型','古言','现言','情感','重生','悬疑','治愈'].map(x=>`<button class="chip ${editorFilter===x?'on':''}" data-action="genre-${x}">${x}</button>`).join('')}</div><div class="panel table-wrap"><table><thead><tr><th>收藏</th><th>编辑 / 平台</th><th>收稿类型</th><th>投稿邮箱</th><th>收稿备注</th></tr></thead><tbody id="editor-rows">${editorRows()}</tbody></table><div class="list-footer"><span>支持 Excel / CSV 导入导出</span><span>当前为示例资料库</span></div></div>`;}
const favorites=new Set([0,1]);
function editorRows(q='',favoriteOnly=false){return editors.map((e,i)=>({e,i})).filter(({e,i})=>(editorFilter==='全部类型'||e.includes(editorFilter))&&e.join('').includes(q)&&(!favoriteOnly||favorites.has(i))).map(({e,i})=>`<tr><td><button class="favorite ${favorites.has(i)?'on':''}" data-action="favorite-${i}" aria-label="${favorites.has(i)?'取消收藏':'收藏'}${e[0]}">${icon('star')}</button></td><td><b style="font-weight:500">${e[0]}</b><small class="muted" style="display:block;font-size:10px">${e[1]}</small></td><td><span class="badge gray">${e[2]}</span> <span class="badge gray">${e[3]}</span></td><td class="mono" style="font-size:10px">${e[4]}</td><td class="muted" style="font-size:11px">${e[5]}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">没有匹配的编辑</td></tr>';}
function accountsPage(){return heading('发件邮箱','管理发件身份，随时了解邮箱的发送状态。',button('添加邮箱','add-account','plus','primary'))+`<div class="account-grid">${['QQ','163'].map((a,i)=>`<article class="panel account-card"><div class="account-head"><div class="provider">${a}</div><div><h3>linjian${i?'02':''}@example.com</h3><p>发件笔名：林间</p></div>${badge('已启用')}</div><div class="progress-line"><span>今日已发送</span><b>${i?'10':'14'} <span>/ 80 封建议额度</span></b></div><div class="progress"><i style="width:${i?'12.5':'17.5'}%"></i></div><div class="account-bottom"><small>SMTP 已配置 · 回复检查已开启</small>${button('邮箱设置','account-settings-'+i,'settings','small')}</div></article>`).join('')}</div><div class="wizard-note">${icon('shield')} 新建计划时可以单独选择参与发送的邮箱。多个邮箱按顺序轮流发送。</div>`;}
function logsPage(){return heading('发送记录','每一封投稿，都有迹可循。',button('导出记录','export','download'))+`<div class="filters"><div class="tabs"><button class="on" data-action="log-all">全部记录</button><button data-action="log-success">发送成功</button><button data-action="log-error">发送失败</button></div><label class="muted" style="font-size:11px">投稿计划 <select id="log-plan" aria-label="按计划筛选"><option>全部计划</option><option>山雨退婚</option><option>春山来信</option></select></label></div><div class="panel table-wrap"><table><thead><tr><th>发送时间</th><th>投稿计划</th><th>收件编辑</th><th>发件邮箱</th><th>发送结果</th></tr></thead><tbody id="log-rows">${logRows()}</tbody></table></div>`;}
function logRows(){return editors.map((e,i)=>`<tr><td class="mono">09/23 14:${String(32-i*3).padStart(2,'0')}:08</td><td>山雨退婚</td><td>${e[0]}<small class="muted" style="display:block;font-size:10px">${e[4]}</small></td><td style="font-size:11px">linjian@example.com</td><td>${badge('发送成功')}</td></tr>`).join('');}
function statsPage(){return heading('投稿统计','回看投递与反馈，为下一次投稿积累经验。',`<span class="badge gray">近 7 天 · 示例</span>`)+metrics([['累计发送','156','来自 4 个投稿计划','send','go-plans'],['人工回复','12','编辑直接反馈','inbox','go-replies'],['过稿回复','3','已手动标记','check','accepted'],['发送失败','2','查看记录核对原因','alert','go-logs']])+`<section class="panel chart-panel"><div class="section-head"><h2>每日投递</h2><span class="muted" style="font-size:11px"><i class="dot"></i> 成功发送</span></div><div class="chart" role="img" aria-label="9月17日至23日成功发送分别为18、26、12、30、28、18、24封">${[18,26,12,30,28,18,24].map(v=>`<div class="chart-col"><b>${v}</b><i style="height:${v*5}px"></i></div>`).join('')}</div><div class="chart-labels">${[17,18,19,20,21,22,23].map(x=>`<span>09/${x}</span>`).join('')}</div></section>`;}
function settingsPage(){return heading('设置','让投稿工具按照你的习惯工作。',button('保存设置','save-settings','check','primary'))+`<div class="settings-layout"><div class="settings-nav">${['发送设置','内容保护','回复检查','启动与关闭','数据与备份'].map(x=>`<button class="${settingsTab===x?'on':''}" data-action="settings-${x}">${x}</button>`).join('')}</div><div class="panel settings-panel"><h2>${settingsTab}</h2>${settingsTab==='发送设置'?`<div class="setting-row"><div><h3>发送失败重试次数</h3><p>作为新建计划的默认重试次数。</p></div><input aria-label="重试次数" type="number" value="3" min="0" max="10" style="width:70px"></div><div class="setting-row"><div><h3>随机发送间隔</h3><p>在每个投稿计划中单独设置，默认 100–240 秒。</p></div>${button('新建计划','new','','small')}</div>`:settingsTab==='数据与备份'?`<div class="setting-row"><div><h3>数据库备份</h3><p>包含邮箱、编辑、投稿计划和发送记录。</p></div>${button('创建备份','backup','download','small')}</div>`:settingsTab==='回复检查'?`<div class="setting-row"><div><h3>检查间隔</h3><p>定期检查启用邮箱的编辑回复，单位：分钟。</p></div><input aria-label="检查间隔" type="number" value="10" min="1" style="width:70px"></div>`:`<label class="setting-row"><div><h3>${settingsTab==='内容保护'?'发送时微调正文':'关闭窗口后在托盘运行'}</h3><p>${settingsTab==='内容保护'?'插入不可见空格，降低内容完全重复的概率。':'让正在执行的投稿计划继续发送。'}</p></div><input type="checkbox" checked></label>${settingsTab==='启动与关闭'?'<label class="setting-row"><div><h3>开机后自动启动</h3></div><input type="checkbox"></label>':''}`}</div></div>`;}
function aboutPage(){return heading('关于','为创作者，少一点琐碎，多一点专注。')+`<article class="panel about-panel"><img src="../../src/assets/logo.png" alt="熊猫投稿"><h2>熊猫投稿</h2><span class="mono muted">NovelSub · v0.2.5</span><p>一款面向小说作者的本地桌面投稿工具。<br>管理作品与编辑，让每次投稿清晰、有序。</p><span class="badge">开源免费 · 数据保存在本机</span><p style="font-size:11px">当前为界面设计预览，所有人物、邮箱与记录均为示例。</p></article>`;}
function render(){document.querySelectorAll('[data-page]').forEach(b=>{b.setAttribute('aria-label',names[b.dataset.page]);b.title=names[b.dataset.page];b.classList.toggle('active',b.dataset.page===page);if(b.dataset.page===page)b.setAttribute('aria-current','page');else b.removeAttribute('aria-current')});$('#crumb-name').textContent=names[page];$('#main').innerHTML=({dashboard,plans:plansPage,replies:repliesPage,editors:editorsPage,groups:groupsPage,accounts:accountsPage,logs:logsPage,stats:statsPage,settings:settingsPage,about:aboutPage})[page]();hydrate();}
function go(p){page=p;history.replaceState(null,'','#'+p);render();window.scrollTo(0,0);}
let toastTimer;function toast(msg){$('#toast').textContent=msg;$('#toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').classList.remove('show'),3200);}
let draft={title:'山月不归',words:'12600',genre:'古言',body:'编辑老师您好：\n\n现将短篇小说《山月不归》投至贵处。作品共 12,600 字，古言情感题材，全文已完结，随信附上正文。\n\n感谢审阅，期待您的回复。\n林间',subject:'投稿：《山月不归》｜12,600 字｜古言',editors:[0,1,2],accounts:[0],min:100,max:240,schedule:'立即发送',attachment:''},step=1,editingIndex=null;
function rememberDraft(){for(const key of ['title','words','genre','body','subject','min','max','schedule']){const el=$('#draft-'+key);if(el)draft[key]=el.value;}if(step===2)draft.editors=[...document.querySelectorAll('[name=recipient]:checked')].map(x=>+x.value);if(step===3)draft.accounts=[...document.querySelectorAll('[name=account]:checked')].map(x=>+x.value);}
function wizard(){const dialog=$('#wizard');dialog.innerHTML=`<header class="dialog-header"><div><h2 id="wizard-title">${editingIndex===null?'新建投稿计划':'编辑投稿计划'}</h2><p>把作品、收件人与发送安排，分三步准备好。</p></div><div class="actions">${button('保存草稿','save-draft','','small')}<button class="icon-button" data-action="close-wizard" aria-label="关闭投稿编辑器">${icon('close')}</button></div></header><div class="stepper">${['作品与邮件','选择收稿编辑','发送安排'].map((x,i)=>`<button class="${step===i+1?'on':''}" data-action="step-${i+1}"><i>${i+1<step?'✓':i+1}</i>${x}</button>`).join('')}</div><div class="wizard-content">${step===1?`<div class="form-grid"><section class="form-panel"><h3>作品信息</h3><label class="field">作品名称<input id="draft-title" value="${esc(draft.title)}" placeholder="填写作品名称"></label><div class="form-row"><label class="field">作品字数<input id="draft-words" type="number" min="1" value="${draft.words}"></label><label class="field">作品类型<select id="draft-genre">${['古言','现言','悬疑','情感'].map(x=>`<option ${draft.genre===x?'selected':''}>${x}</option>`).join('')}</select></label></div><label class="field">作品附件<span class="attachment">${icon('file')}<input type="file" id="draft-file" accept=".doc,.docx,.txt,.pdf" aria-label="选择作品附件"></span><small id="attachment-name" class="muted">${draft.attachment?esc(draft.attachment):'选择本地稿件，预览不会上传文件'}</small></label><p class="muted" style="font-size:10px">笔名：林间 · 完稿状态由邮件正文说明</p></section><section class="form-panel"><div class="section-head"><h3>投稿邮件</h3><span class="badge gray">常规问候模板</span></div><label class="field">邮件主题<input id="draft-subject" value="${esc(draft.subject)}"></label><label class="field">邮件正文<textarea id="draft-body" rows="7">${esc(draft.body)}</textarea></label><p class="muted" style="font-size:10px">下一步选择编辑，最后确认发件邮箱与发送间隔。</p></section></div>`:step===2?`<div class="section-head"><h2>选择这次投稿的编辑</h2><span class="muted" style="font-size:11px" id="selected-count">已选 ${draft.editors.length} 位</span></div><div class="editor-chips"><button class="chip" data-action="select-group-0">从「古言常投」选入</button><button class="chip" data-action="select-group-1">从「现言常投」选入</button><button class="chip" data-action="select-none">清空选择</button></div><div class="recipient-grid">${editors.map((e,i)=>`<label class="check-row"><input type="checkbox" name="recipient" value="${i}" ${draft.editors.includes(i)?'checked':''}><span class="avatar">${e[0][0]}</span><span>${e[0]} · ${e[1]}<small>${e[4]}</small></span><span class="badge gray">${e[2]}</span></label>`).join('')}</div><div class="wizard-note">这次勾选只影响当前计划，编辑组里的成员保持不变。</div>`:`<div class="form-grid"><section class="form-panel"><h3>选择发件邮箱</h3>${['linjian@example.com','linjian02@example.com'].map((x,i)=>`<label class="check-row"><input type="checkbox" name="account" value="${i}" ${draft.accounts.includes(i)?'checked':''}><span>${x}<small>${i?'163':'QQ'} 邮箱 · 今日 ${i?10:14} / 80 封建议额度</small></span></label>`).join('')}<div class="wizard-note">勾选的邮箱轮流发送，每封邮件分别投递。</div></section><section class="form-panel"><h3>发送节奏</h3><div class="form-row"><label class="field">最短间隔（秒）<input id="draft-min" type="number" min="1" max="86400" value="${draft.min}"></label><label class="field">最长间隔（秒）<input id="draft-max" type="number" min="1" max="86400" value="${draft.max}"></label></div><label class="field">发送方式<select id="draft-schedule">${['立即发送','定时发送','循环投稿'].map(x=>`<option ${x===draft.schedule?'selected':''}>${x}</option>`).join('')}</select></label><label class="field ${draft.schedule==='定时发送'?'':'hidden'}" id="schedule-field">发送时间<input type="datetime-local" id="schedule-date" value="2026-09-24T09:00"></label><p class="muted" style="font-size:11px">作品：《${esc(draft.title)}》 · 已选 ${draft.editors.length} 位编辑</p></section></div><div class="wizard-note">${icon('info')} 设计预览：测试与开始发送仅演示交互，不会发出邮件。</div>`}</div><footer class="dialog-footer"><span>第 ${step} / 3 步</span><div class="actions">${step>1?button('上一步','prev'):''}${step===3?button('测试发送','test-send')+button('开始发送','simulate-send','send','primary'):button('下一步','next','arrow','primary')}</div></footer>`;if(!dialog.open)dialog.showModal();}
function showDetail(i){const p=plans[i];$('#detail').innerHTML=`<header class="dialog-header"><div><h2 id="detail-title">投稿详情</h2><p>从计划准备到每一次投递</p></div><button class="icon-button" data-action="close-detail" aria-label="关闭详情">${icon('close')}</button></header><div class="detail-content"><div class="plan-title-row">${book(p)}<div><div class="plan-title">${esc(p.title)}</div><div class="plan-meta">${p.genre} · ${p.words} 字</div></div>${badge(i===0&&paused?'已暂停':p.state)}</div><div class="progress-line"><span>投递进度</span><b>${p.sent} / ${p.total}</b></div><div class="progress"><i style="width:${p.sent/p.total*100}%"></i></div><div class="timeline">${p.sent?`<div><time>14:32</time>邮件已发送至青禾 · 知微故事</div><div><time>14:29</time>邮件已发送至南枝 · 拾光阅读</div><div><time>14:26</time>邮件已发送至木棉 · 望山文学</div>`:`<div><time>已保存</time>${p.state==='草稿'?'作品资料已保存，等待完善发送安排':'等待设定的发送时间'}</div>`}<div><time>${p.time}</time>${p.mode}</div></div><div class="wizard-note">发送间隔 100–240 秒 · 失败最多重试 3 次</div></div><footer class="dialog-footer"><span>示例投递记录</span>${button('查看全部记录','detail-logs','arrow')}</footer>`;$('#detail').showModal();}
function simpleDialog(title,body,action='close-detail',label='完成'){const d=$('#detail');d.innerHTML=`<header class="dialog-header"><h2 id="detail-title">${title}</h2><button class="icon-button" data-action="close-detail" aria-label="关闭">${icon('close')}</button></header><div class="detail-content">${body}</div><footer class="dialog-footer"><span>仅用于设计预览</span>${button(label,action,'','primary')}</footer>`;if(!d.open)d.showModal();}
function validateStep(){rememberDraft();if(step===1&&(!draft.title.trim()||!draft.body.trim()||!draft.subject.trim())){toast('请填写作品名称、邮件主题和正文');return false;}if(step===2&&!draft.editors.length){toast('请至少选择一位编辑');return false;}return true;}
document.addEventListener('click',e=>{const nav=e.target.closest('[data-page]');if(nav){go(nav.dataset.page);return;}const el=e.target.closest('[data-action]');if(!el)return;const a=el.dataset.action;
if(a.startsWith('go-'))return go(a.slice(3));
if(a==='new'||a.startsWith('edit-')){editingIndex=a==='new'?null:+a.slice(5);if(editingIndex!==null){draft.title=plans[editingIndex].title;draft.words=plans[editingIndex].words.replace(',','');}step=1;return wizard();}
if(a==='pause'){paused=!paused;render();return toast(paused?'示例计划已暂停':'示例计划已继续');}
if(a==='detail-logs'){$('#detail').close();return go('logs');}
if(/^detail-\d+$/.test(a))return showDetail(+a.slice(7));
if(/^reply-\d+$/.test(a)){selectedReply=+a.slice(6);return go('replies');}
if(a.startsWith('plan-filter-')){planFilter=a.slice(12);return render();}
if(a.startsWith('reply-filter-')){replyFilter=a.slice(13);return render();}
if(a.startsWith('genre-')){editorFilter=a.slice(6);return render();}
if(a.startsWith('settings-')){settingsTab=a.slice(9);return render();}
if(a==='close-wizard'){rememberDraft();return $('#wizard').close();}if(a==='close-detail')return $('#detail').close();
if(a==='next'){if(!validateStep())return;step++;return wizard();}if(a==='prev'){rememberDraft();step--;return wizard();}
if(a.startsWith('step-')){let target=+a.slice(5);if(target>step&&!validateStep())return;rememberDraft();step=target;return wizard();}
if(a==='save-draft'){rememberDraft();if(!draft.title.trim())return toast('请先填写作品名称');const p={title:draft.title,short:draft.title.slice(0,2),genre:draft.genre,words:draft.words,state:'草稿',sent:0,total:draft.editors.length||1,color:'blue',time:'刚刚',mode:'尚未发送'};if(editingIndex!==null)plans[editingIndex]=p;else plans.push(p);$('#wizard').close();go('plans');return toast('示例草稿已保存，仅在本次预览中保留');}
if(a==='select-none'||a.startsWith('select-group-')){draft.editors=a==='select-none'?[]:a.endsWith('0')?[0,2,5]:[1,3,4];return wizard();}
if(a==='test-send')return toast('预览：测试邮件将发送到第一个勾选的发件邮箱');
if(a==='simulate-send'){rememberDraft();if(!draft.accounts.length)return toast('请至少选择一个发件邮箱');if(!draft.editors.length)return toast('请先选择收稿编辑');if(!(Number(draft.min)>=1&&Number(draft.max)<=86400&&Number(draft.max)>=Number(draft.min)))return toast('请填写有效的 1–86400 秒间隔，最短不能大于最长');$('#wizard').close();return toast('发送流程演示完成，没有发送任何邮件');}
if(a==='accepted'){replyFilter='过稿回复';return go('replies');}
if(a==='mark-accepted'){replies[selectedReply].tag=replies[selectedReply].tag==='已标记过稿'?'人工回复':'已标记过稿';render();return toast('示例过稿标记已更新');}
if(a.startsWith('favorite-')&&a!=='favorite-reply'){const i=+a.slice(9);favorites.has(i)?favorites.delete(i):favorites.add(i);$('#editor-rows').innerHTML=editorRows($('#editor-search')?.value||'');return;}
if(a==='favorite-reply'){el.classList.toggle('on');el.innerHTML=icon('star')+(el.classList.contains('on')?'已收藏':'收藏这位编辑');return toast('已更新示例收藏状态');}
if(a==='scan'){el.disabled=true;el.innerHTML=icon('refresh')+'正在检查…';setTimeout(()=>{render();toast('示例检查完成，没有新增回复');},800);return;}
if(a==='editors-all'){editorFilter='全部类型';return render();}
if(a==='editors-favorites'){$('#editor-rows').innerHTML=editorRows('',true);document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('on',x===el));return;}
if(a==='editors-groups')return simpleDialog('编辑组',`<div class="check-row">${icon('users')}<span>古言常投<small>青禾、木棉、时雨 · 3 位编辑</small></span></div><div class="check-row">${icon('users')}<span>现言常投<small>南枝、白露、初夏 · 3 位编辑</small></span></div>`);
if(a==='add-editor')return simpleDialog('添加编辑',`<label class="field">编辑名称<input placeholder="例如：青禾"></label><label class="field">平台<input placeholder="所属平台或栏目"></label><label class="field">投稿邮箱<input type="email" placeholder="editor@example.com"></label><label class="field">收稿备注<textarea placeholder="收稿类型、字数、审稿周期"></textarea></label>`,'mock-save','保存编辑');
if(a==='data-editors')return simpleDialog('编辑资料管理',`<p class="muted" style="font-size:12px;margin-bottom:20px">导入常用编辑资料，或导出一份留存。</p><div class="actions">${button('导入 Excel / CSV','mock-import','file')}${button('导出示例 CSV','export','download')}</div>`);
if(a==='add-account'||a.startsWith('account-settings-'))return simpleDialog(a==='add-account'?'添加发件邮箱':'邮箱设置',`<label class="field">邮箱地址<input type="email" placeholder="author@example.com" value="${a==='add-account'?'':'linjian@example.com'}"></label><label class="field">邮箱授权码<input type="password" placeholder="请输入邮箱授权码"></label><label class="field">发件笔名<input value="林间"></label><label class="check-row"><input type="checkbox" checked>启用 IMAP 回复检查</label><p class="muted" style="font-size:11px">预览无需填写真实邮箱或授权码。</p>`,'mock-save','保存邮箱');
if(a==='mock-save'){$('#detail').close();return toast('已演示保存操作，不会写入真实数据');}
if(a==='mock-import')return toast('这里将打开文件选择器，导入 Excel / CSV 编辑资料');
if(a==='save-settings')return toast('示例设置已保存，正式软件设置保持不变');
if(a==='backup')return toast('预览不连接数据库，没有创建真实备份');
if(a.startsWith('log-')){document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('on',x===el));$('#log-rows').innerHTML=a==='log-error'?'<tr><td colspan="5" class="empty">今日没有发送失败的记录</td></tr>':logRows();return;}
if(a==='export'){const rows=page==='editors'?[['编辑','平台','类型','类型2','邮箱','备注'],...editors]:[['时间','作品','编辑','结果'],...editors.map(e=>['2026-09-23','山雨退婚',e[0],'发送成功'])];const blob=new Blob(['\ufeff'+rows.map(row=>row.map(x=>'"'+x.replaceAll('"','""')+'"').join(',')).join('\r\n')],{type:'text/csv;charset=utf-8'});const url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download='熊猫投稿-示例数据.csv';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);return toast('已导出预览中的示例数据');}
});
document.addEventListener('input',e=>{if(e.target.id==='plan-search')$('#plan-rows').innerHTML=planRows(plans.filter(p=>(planFilter==='全部计划'||p.state===planFilter)&&p.title.includes(e.target.value)));if(e.target.id==='editor-search')$('#editor-rows').innerHTML=editorRows(e.target.value);});
document.addEventListener('change',e=>{if(e.target.name==='recipient'){rememberDraft();$('#selected-count').textContent=`已选 ${draft.editors.length} 位`;}if(e.target.id==='draft-schedule'){rememberDraft();$('#schedule-field').classList.toggle('hidden',draft.schedule!=='定时发送');}if(e.target.id==='draft-file'){draft.attachment=e.target.files[0]?.name||'';$('#attachment-name').textContent=draft.attachment||'未选择附件';}if(e.target.id==='log-plan')$('#log-rows').innerHTML=e.target.value==='春山来信'?'<tr><td colspan="5" class="empty">当前示例仅展示《山雨退婚》的发送记录</td></tr>':logRows();});
window.addEventListener('hashchange',()=>{const p=location.hash.slice(1);if(names[p]){page=p;render();}});
page=names[location.hash.slice(1)]?location.hash.slice(1):'dashboard';render();

function groupsPage(){return heading("编辑组","将常投编辑整理成投稿名单。")+"<p>正在加载编辑组…</p>";}
